# 아파트 전세가율 예측 연구 — 학술지 초록용 상세 정리

> 본 문서는 학술지 초록·본문 작성을 위한 **원자료(상세 기술)**이다.  
> 요약·초록 문장은 별도로 축약할 예정이며, 여기에는 아이디어·방법·실험·결과를 빠짐없이 기록한다.

---

## 1. 연구 아이디어 및 문제 정의

### 1.1 연구 배경

한국 주택시장에서 **전세**는 매매와 함께 핵심 거래 형태이다. 실수요자·중개·금융 관점에서 특정 아파트의 **적정 전세가(또는 전세가율)**를 추정하는 수요가 있으나, 동일 시점·동일 단지·동일 면적의 매매·전세 거래가 항상 동시에 존재하지는 않는다.

본 연구는 공공 실거래가 데이터(매매·전세)를 결합하여, **매매가격과 주택 속성으로부터 전세가격(또는 전세가율)을 예측**하는 머신러닝 파이프라인을 구축하는 것을 목표로 한다.

### 1.2 연구 목적

- 성남시(분당) 아파트 **매매 실거래가**와 **전세 실거래가**를 결합한 학습용 데이터셋 구축
- **시계열(시점) 기반** 학습·검증·평가 구조 적용
- **XGBoost** 회귀 모델로 전세가율(`jeonseRatio`)을 예측하고, 매매가와 곱해 전세가를 환산
- 단순 평균 오차뿐 아니라 **오차 분포·Worst case**까지 포함한 평가
- 비정상(허수) 거래·이상치 제거, 연도 가중치, 하이퍼파라미터 탐색 등 **적용형 ML(Applied ML)** 개선 절차 정리

### 1.3 예측 대상(타깃)의 정의

초기에는 전세가격(`jeonsePrice`) 자체를 직접 예측하는 방향을 고려했으나, 최종적으로는 다음 **비율 기반 타깃**을 사용한다.

\[
\text{jeonseRatio} = \frac{\text{jeonsePrice}}{\text{salePrice}}
\]

예측 후 전세가 환산:

\[
\widehat{\text{jeonsePrice}} = \widehat{\text{jeonseRatio}} \times \text{salePrice}
\]

**학술적 표현:** Ratio-based Target Engineering / Target Transformation  
(매매가 스케일에 종속되지 않는 상대 비율을 학습하고, 사후적으로 절대 전세가로 복원)

### 1.4 연구 범위(공간·시간)

| 항목 | 내용 |
|------|------|
| 지역 | 성남시 분당구(데이터 상 `region = 분당`) |
| 기간 | 2020 ~ 2025년 실거래 데이터 |
| 거래 유형 | 아파트 매매 + 아파트 전세(월세 제외) |
| 최종 데이터셋 규모 | 약 **8,050 rows** (`merged_dataset.csv`) |

---

## 2. 데이터 및 전처리(ETL)

### 2.1 원천 데이터

국토교통부 등에서 제공하는 아파트 실거래가 Excel을 연도별로 로드한다.

- 매매: `아파트(매매)_실거래가_{year}.xlsx`
- 전세: `아파트(전세)_실거래가_{year}.xlsx` (전월세구분 = “전세”만 사용)
- Excel 헤더 시작 행: 12행

### 2.2 단지 식별자(apt_id) 및 면적대(area_group)

- `apt_id` = 시군구(공백 제거) + 본번 + 부번  
  → 동일 단지를 매매·전세 간 조인하기 위한 키
- `area_group` = 전용면적(㎡)을 반올림한 값  
  → 동일 단지 내에서도 면적대가 다르면 전세가율이 다를 수 있음을 반영

### 2.3 매매–전세 결합(Matching) 전략

**초기 문제:** 동일 연도(`saleYear == year`)만 매칭하면 샘플이 약 **2,000건** 수준으로 부족하고, 같은 단지라도 해당 연도 거래가 없는 경우가 많음.

**해결:** 매매 연도와 전세 연도 차이를 **±1년**까지 허용.

\[
|\text{saleYear} - \text{year}| \le 1
\]

그 결과 데이터셋이 약 **2,000 → 약 8,000 rows**로 확장됨.

조인 키: `(apt_id, area_group)`  
중복 제거 후 inner join → 허용 오차 필터.

### 2.4 학습 기간 제한(비정상성 / Non-stationarity)

**초기 시도:** 2018~2025까지 더 긴 기간을 쓰면 표본은 늘지만 성능이 **악화**.

**해석:** 부동산 시장은 stationary하지 않으며, 금리·규제·경기 국면에 따라 매매–전세 관계가 크게 변함. 오래된 국면 데이터를 그대로 넣으면 최근 패턴 학습을 방해할 수 있음.

**최종 사용 기간:** 2020~2025 (데이터셋 구축 기준 연도 목록)

### 2.5 파생 피처(Feature Engineering)

| 피처 | 정의 / 의미 | 학술적 표현 |
|------|-------------|-------------|
| `price_per_m2` | `salePrice / area` | 단위면적당 매매가 |
| `jeonseRatio` | `jeonsePrice / salePrice` | 전세가율(타깃) |
| `match_gap_year` | `saleYear - year` | Temporal Distance Feature (매칭 시차) |
| `buildingAge` | `year - constructionYear` | 건물 연식 |
| `last_jeonse_ratio` | 동일 `apt_id`+`area_group`의 **직전 시점** 전세가율 (`shift(1)`) | Lag / Autoregressive Feature |
| `last_3_mean_jeonse_ratio` | 동일 단지·면적대 직전 최대 3건 평균(롤링, `shift` 후) | Rolling / Moving Average Lag |
| `price_percentile_in_dong` | 동·연도 내 `price_per_m2`의 백분위 순위 | Submarket / Relative Price Rank |

`last_jeonse_ratio`가 없는 행은 학습에서 제거(시계열 래그 확보를 위한 결측 처리).

### 2.6 데이터셋 단계의 1차 이상치 필터

ETL(`build_dataset.py`) 단계에서 전세가율 극단값 일부를 이미 제거:

\[
0.1 \le \text{jeonseRatio} \le 0.95
\]

(이후 학습 단계에서 **추가 이상치 제거**를 한 번 더 적용 — 3.4절 참고)

### 2.7 Data Leakage 방지

실험 중 다음 유형의 피처는 **제외**함:

- `dong_mean_jeonse_ratio`, `apt_mean_jeonse_ratio` 등 **전체 기간 평균**을 한 번에 계산하는 피처

이유: train/test를 섞은 전체 평균을 쓰면 **미래 정보가 학습 피처로 유입**될 위험이 있음.  
래그·롤링은 **과거 방향으로만** (`shift`) 계산하여 누수를 완화.

---

## 3. 방법론 및 사용 기술

### 3.1 전체 파이프라인 개요

```
[원천 Excel]
    → ETL / 매매–전세 매칭 / 피처 생성  →  merged_dataset.csv
    → 이상치 필터 (학습 단계)
    → 범주형 인코딩 (원핫) + 희소 카테고리 “기타” 통합
    → Temporal Holdout (train/val/test)
    → XGBoost 1차 학습 (전체 피처)
    → Feature Importance 상위 K개 재학습
    → 전세가율 예측 → 전세가 환산 → 평가
    → (선택) 하이퍼파라미터 랜덤 탐색
    → 모델 저장 (joblib/pickle) + FastAPI 서빙
```

### 3.2 사용 기술 스택

| 구분 | 기술 |
|------|------|
| 언어 | Python 3 |
| 데이터 처리 | pandas, openpyxl |
| 모델 | XGBoost Regressor (`xgboost`) |
| 평가 | scikit-learn (`MAE`, `MSE`→RMSE), 자체 MAPE·백분위 오차 |
| 모델 직렬화 | joblib / pickle |
| API | FastAPI, uvicorn, pydantic, CORS |
| 하이퍼파라미터 탐색 | 자체 랜덤 서치 스크립트 (`tune_xgboost.py`, 20 trials) |

### 3.3 범주형 변수 처리

- 대상: `apartmentName`, `dong`, (존재 시) `region`
- **희소 카테고리:** 출현 횟수 `< 15`인 단지/동 → `"기타"`로 통합 (`MIN_SAMPLES_FOR_CATEGORY = 15`)
- **원핫 인코딩** (`pd.get_dummies`)

### 3.4 학습 단계 전세가율 이상치 제거

**동기:** Worst predictions에 `매매 1.68억 vs 전세 0.38억`(전세가율 ≈ 0.22)처럼 **시세 대비 비정상적으로 낮은 전세**가 포함됨. 가족·지인 거래 등 **허수/비시장 거래**로 해석 가능하며, 이를 평가·학습에 그대로 두면 평균 MAPE가 왜곡됨.

**두 가지 제거 모드를 구현:**

1. **Bounds 모드**  
   - `jeonseRatio < 0.25` 제거  
   - `jeonseRatio > 0.90` 제거  

2. **Percentile 모드 (현재 코드 기본)**  
   - 하위 `OUTLIER_PERCENTILE_LOW`% 제거  
   - 상위 `OUTLIER_PERCENTILE_HIGH`% 제거  
   - 현재 설정 예: 하위 **20%**, 상위 **10%** 제거 (유효 구간은 실행 시 분위수로 출력)

이상치 제거는 **train/val/test 분할 이전**에 전체 데이터에 적용되어, 학습과 평가가 동일 분포 정의 위에서 이뤄짐.

### 3.5 시계열 분할 (Temporal Holdout Validation)

랜덤 셔플 분할이 아닌 **연도 기준 홀드아웃**:

| 분할 | 조건 |
|------|------|
| Train | `year ≤ 2023` |
| Validation | `year == 2024` |
| Test | `year == 2025` |

→ 미래 시점 예측 성능을 모사하기 위한 설정. Early stopping은 validation(2024)을 사용.

### 3.6 연도별 샘플 가중치 (Sample Weighting)

오래된 연도의 영향력을 줄이기 위해 train 샘플에 연도별 가중치 부여:

| year | weight |
|------|--------|
| 2023 | 1.0 |
| 2022 | 0.6 |
| 2021 | 0.5 |
| 2020 | 0.4 |
| (기타 default) | 0.4 |

(코드상 2024/2025 키도 정의되어 있으나, train 마스크는 `≤2023`이므로 실질 적용은 2020~2023)

### 3.7 모델: XGBoost Regressor

**학습 절차 (2-stage):**

1. **1차 학습:** 전체 피처로 학습 → Feature Importance 산출  
2. **재학습:** Importance 상위 **K=50**개 피처만 사용해 재학습  
   - 단지명 원핫 등으로 피처가 폭증하는 상황에서 **노이즈 피처 축소** 목적

**Early stopping:** `early_stopping_rounds = 50` (validation set 기준)

**수치 피처(대표):**  
`area`, `floor`, `buildingAge`, `salePrice`, `price_per_m2`, `match_gap_year`, `last_jeonse_ratio`,  
(+ 존재 시) `price_percentile_in_dong`, `last_3_mean_jeonse_ratio`, `saleYear`  
+ 원핫된 `apartmentName_*`, `dong_*`, `region_*`

### 3.8 하이퍼파라미터 (랜덤 탐색 후 선정)

`tune_xgboost.py`에서 **20회 랜덤 탐색**, 목적함수 = **Test MAPE 최소화**.

탐색 공간 예:

- `n_estimators`: 300~700  
- `max_depth`: 3~6  
- `learning_rate`: 0.03~0.07  
- `subsample`, `colsample_bytree`: 0.7~0.9  
- `min_child_weight`: 5~15  
- `reg_alpha`, `reg_lambda`: L1/L2 정규화 구간

코드에 반영된 **최종(권장) 파라미터** (주석: Test MAPE 8.78% 기준 최적):

```text
n_estimators = 400
max_depth = 6
learning_rate = 0.07
subsample = 0.8
colsample_bytree = 0.85
min_child_weight = 12
reg_alpha = 0.2
reg_lambda = 1.0
random_state = 42
```

### 3.9 서빙(부가 구현)

FastAPI로 `/predict` 엔드포인트 제공:

- 입력 예: `salePrice`, `area`, `floor`, `buildYear`, `saleYear`
- 출력: `predicted_jeonse_ratio`, `predicted_jeonse_price`

(연구 본문의 핵심은 예측 모델링이며, API는 적용·데모 계층)

---

## 4. 실험 설계

### 4.1 실험이 해결하려 한 질문

1. 매매–전세 시차(±1년) 허용이 표본·성능에 어떤 영향을 주는가?  
2. 긴 기간(구 국면) 데이터를 넣을수록 성능이 나빠지는가? (비정상성)  
3. 전세가 **절대값** 대신 **비율(전세가율)** 예측이 유효한가?  
4. 단지명 lookup에 과도하게 의존하는가? (Feature Importance 분석)  
5. 평균 MAPE만으로 성능을 판단하면 왜곡되는가? (오차 분포·Worst case)  
6. 비정상 전세가율(허수 거래) 제거가 Worst case·평균 오차를 개선하는가?  
7. 연도 가중치·래그/롤링 피처·상위 K 재학습·하이퍼파라미터 탐색이 성능을 개선하는가?

### 4.2 실험·개선 단계 (시간순 요약)

| 단계 | 내용 | 관찰 |
|------|------|------|
| A | 동일 연도만 매칭 | 표본 ~2,000, 데이터 부족 |
| B | ±1년 매칭 허용 | 표본 ~8,000으로 확장 |
| C | 2018~ 장기 데이터 포함 | 성능 악화 → 최근 구간으로 제한 |
| D | 타깃을 `jeonseRatio`로 변환 | 스케일 안정화, 전세가 환산 평가 |
| E | `match_gap_year`, `last_jeonse_ratio` 추가 | 시차·자기상관 반영 |
| F | 동/단지 전체평균 피처 배제 | Data leakage 방지 |
| G | Temporal holdout (≤2023 / 2024 / 2025) | 시점 일반화 평가 |
| H | MAE/RMSE/MAPE + median/p75/p90/p95 + Worst 20 | 평균 지표의 한계 보완 |
| I | 희소 단지 “기타” + Importance Top-50 재학습 | 고차원 원핫 노이즈 완화 |
| J | 연도별 sample weight | 최근 국면 강조 |
| K | `price_percentile_in_dong`, `last_3_mean_jeonse_ratio` | 서브마켓·단기 추세 반영 |
| L | 전세가율 bounds / percentile 이상치 제거 | 허수 거래·극단값 완화 |
| M | 하이퍼파라미터 랜덤 탐색 20회 | Test MAPE 기준 최적 조합 선정 |

### 4.3 평가 지표

전세가(절대값, 만원 단위) 기준으로 평가:

| 지표 | 의미 |
|------|------|
| MAE | 평균 절대 오차 |
| RMSE | 큰 오차에 민감한 제곱근 오차 |
| MAPE | 상대 오차(%) 평균 |
| Median MAPE | 중앙 상대 오차(이상치에 덜 민감) |
| p75 / p90 / p95 | 오차 분포의 꼬리 |
| max | 최대 상대 오차 |
| 가격대별 MAPE | 0~5억 / 5억 이상 구간별 |
| Worst predictions Top-20 | 최대 오차 샘플 정성 분석 |

**평균 지표만 쓰는 것의 한계(연구 내 문제의식):**  
대부분 샘플이 잘 맞아도 소수 극단 오차가 평균 MAPE를 크게 끌어올릴 수 있음 → 분포·Worst case 병행 필수.

### 4.4 Feature Importance로 관찰한 모형 행태

초기 Importance에서 `apartmentName_*`, `dong_*`가 상위에 자주 등장.

해석: 모델이 상당 부분

> “이 단지(또는 동)는 평균적으로 전세가율이 이 정도”

라는 **lookup식 패턴**에 의존할 수 있음.  
이는 부동산 데이터에서 흔한 현상이며, 동시에 **일반화·신규 단지 예측**의 취약점이 될 수 있음.  
→ 래그/상대가격 피처·Top-K 재학습·이상치 제거로 보완을 시도.

---

## 5. 실험 결과

> 참고: 이상치 필터·가중치·하이퍼파라미터가 변경될 때마다 수치가 달라진다.  
> 아래에 **문서화된 대표 결과**와 **코드에 기록된 최적 탐색 결과**를 구분하여 적는다.

### 5.1 대표 베이스라인 결과 (문제정리.md 기록분)

이상치 추가 제거·가중치 차별화 이전 등으로 기록된 테스트 성능:

| 지표 | 값 |
|------|-----|
| Test MAE | ≈ 8,926 (만원) |
| Test RMSE | ≈ 12,563 |
| Test MAPE | ≈ **11.81%** |
| Median MAPE | ≈ **9.83%** |
| p75 | ≈ 16.75% |
| p90 | ≈ 22.94% |
| p95 | ≈ 27.34% |
| max | ≈ **204.5%** |

가격대별(기록분):

- 0~5억: MAPE ≈ 14.96% (n≈238)  
- 5억 이상: MAPE ≈ 11.05% (n≈989)

**해석(당시):**

- 절반의 예측은 대략 **10% 이하** 오차(중앙값 기준)
- 대부분(약 90%)은 **23% 이하** 오차
- 소수 특이 거래가 평균·max를 크게 악화

### 5.2 Worst Prediction 정성 분석 (베이스라인)

Worst 샘플의 공통 경향:

- 전세가율이 **비정상적으로 낮은** 거래
- 고가 아파트에서 전세가 과소·과대 예측이 두드러지는 경우
- 특정 지역(예: 정자동 등) 편중 가능성

대표 예시:

| salePrice | jeonsePrice | 전세가율(대략) | 해석 |
|-----------|-------------|----------------|------|
| 168,000 | 38,000 | ≈ 0.23 | 시세 대비 과소 전세 → 지인/특수 거래 의심 |
| 369,000 | 83,000 | ≈ 0.22 | 동일하게 극단 저비율 |

→ 이러한 샘플을 **예측 평가 대상에서 제외할지**가 후속 실험(이상치 필터)의 직접 동기.

### 5.3 하이퍼파라미터 랜덤 탐색 결과 (코드 주석 기준)

- 탐색 횟수: **20 trials**
- 선정 기준: Test MAPE 최소
- 기록된 최적 성능: Test MAPE ≈ **8.78%**  
  (해당 시점의 전처리·필터·가중치 설정 하에서 측정)

### 5.4 이상치 제거 실험 (연구 과정 중 진행)

**아이디어:**  
`jeonseRatio` 하한·상한(고정 구간) 또는 상·하위 백분위 제거 후 **재학습·재평가**.

구현된 옵션:

- Bounds: `<0.25` 또는 `>0.9` 제거  
- Percentile: 하위 N% · 상위 M% 제거 (예: 5%/5%, 이후 설정 변경으로 20%/10% 등)

기대 효과:

- Worst case max 오차 감소  
- 평균 MAPE·꼬리 분위(p90/p95) 안정화  
- “시장 거래”에 가까운 분포에서의 예측력 평가

(정확한 수치 표는 **최종 채택 필터 설정으로 학습을 한 번 더 돌린 로그**를 기준으로 초록에 기입하는 것을 권장.)

### 5.5 데이터 기술통계 (현재 `merged_dataset.csv`)

| 항목 | 값 |
|------|-----|
| 행 수 | 8,050 |
| 연도 | 2020~2025 |
| jeonseRatio 평균 | ≈ 0.504 |
| jeonseRatio 표준편차 | ≈ 0.118 |
| jeonseRatio 중앙값 | ≈ 0.498 |
| jeonseRatio 범위(ETL 후) | 약 0.10 ~ 0.95 |

### 5.6 결과 종합 해석 (초록용 핵심 메시지 후보)

1. **매매–전세 ±1년 매칭**으로 표본 부족을 완화하면서도, **시계열 홀드아웃**으로 미래 시점 일반화를 평가할 수 있다.  
2. **전세가율(비율) 예측 → 전세가 환산**은 가격 스케일이 다른 아파트에 대해 안정적인 타깃 설계이다.  
3. **래그·롤링 전세가율**, **매칭 시차**, **동 내 상대 가격 순위** 등은 단순 단지명 lookup을 보완하는 시계열·서브마켓 정보이다.  
4. 평균 MAPE만으로는 성능을 과대/과소평가하기 쉬우며, **중앙값·꼬리 분포·Worst case** 분석이 필수이다.  
5. 극단적으로 낮은/높은 전세가율은 **비시장 거래**일 가능성이 있어, 이상치 제거 후 재학습이 평가의 현실성을 높인다.  
6. XGBoost + 조기종료 + Top-K 재학습 + 랜덤 탐색으로 **Test MAPE 한 자릿수대(약 8.8%대)** 수준의 성능을 확인한 바 있다(설정 의존).

---

## 6. 관련 연구(참고 문헌 — 프로젝트 내 정리분)

1. 김태영·류두진·박은일 (2023). 기계학습과 설명가능한 인공지능 모형을 이용한 강남의 전세가격 결정요인 분석.  
2. 공석현·오하영 (2023). 머신 러닝 기법을 이용한 주요 지역 매매가격 예측: 전세 제도를 중심으로.  
3. 배성완·유정석 (2018). 머신 러닝 방법과 시계열 분석 모형을 이용한 부동산 가격지수 예측.  
4. 김학현·유환규·오하영 (2023). 딥러닝과 머신러닝을 이용한 아파트 실거래가 예측.

본 연구와의 연결점(초록에 쓸 때):

- (2) 전세·매매 관계를 머신러닝으로 다루는 문제 설정 → **비율 타깃**과 정합  
- (3) 시계열·머신러닝 결합 → **temporal holdout, lag/rolling**과 정합  
- (1)(4) 전세/실거래 예측의 실증 맥락 → 설명력·적용성 논의의 근거

---

## 7. 한계 및 향후 과제

### 7.1 한계

- **지역 한정:** 분당(성남) 중심 → 타 지역 일반화는 미검증  
- **매칭 근사:** ±1년 매칭은 표본을 늘리지만, 동일 시점 시장 조건을 완벽히 반영하지 못함  
- **단지명 의존:** Importance상 단지/동 원핫 비중이 클 수 있음  
- **API 입력 단순화:** 서빙 API는 연구용 전체 피처(단지명, 래그 등)를 모두 받지 않을 수 있음  
- **이상치 정의의 주관성:** bounds vs percentile, 제거 비율에 따라 성능·해석이 달라짐  
- **인과 추론 아님:** 예측 성능 중심이며 전세가 결정요인의 인과 식별은 범위 밖

### 7.2 향후 개선 방향

- Leakage-safe한 `dong_mean_ratio` / `apt_mean_ratio` (과거만 사용)  
- `price_rank_in_dong`, `sale_price_change` 등 추가 피처  
- LightGBM / CatBoost 등 **모델 비교 실험**  
- 이상치 필터 민감도 분석(제거 비율별 MAPE·분포)  
- 지역 확장 또는 다지역 transfer / 지역 임베딩  
- SHAP 등 **설명가능 AI**로 주요 요인 보고

---

## 8. 초록 작성 시 바로 쓸 수 있는 구성 체크리스트

초록에 보통 들어가는 요소 ↔ 본 문서 위치:

| 초록 요소 | 본 문서 절 |
|-----------|------------|
| 배경·필요성 | §1.1, §1.2 |
| 목적 | §1.2, §1.3 |
| 데이터·범위 | §1.4, §2, §5.5 |
| 방법 | §3 전체 |
| 실험 | §4 |
| 결과 | §5 |
| 의의·한계(한 줄) | §5.6, §7 |

권장 한 줄 핵심(초안용, 추후 축약):

> 본 연구는 성남 분당 아파트 매매·전세 실거래가를 ±1년 허용 매칭으로 결합하고, 전세가율을 XGBoost로 예측한 뒤 매매가와 곱해 전세가를 추정하였으며, 시계열 홀드아웃·래그 피처·이상치 제거·하이퍼파라미터 탐색을 통해 테스트 MAPE를 약 한 자릿수대까지 개선하고 오차 분포와 Worst case를 함께 분석하였다.

---

## 부록 A. 주요 파일 맵

| 경로 | 역할 |
|------|------|
| `preprocessing/build_dataset.py` | ETL, 매칭, 피처 생성, CSV 저장 |
| `dataset/merged_dataset.csv` | 학습용 최종 테이블 |
| `training/train_xgboost.py` | 학습·평가·이상치 필터·Top-K 재학습 |
| `training/tune_xgboost.py` | 하이퍼파라미터 랜덤 탐색 20회 |
| `model/xgboost_jeonse_model.pkl` | 학습된 모델 |
| `model/xgboost_feature_cols.pkl` | 사용 피처 목록 |
| `api/app.py` | FastAPI 예측 API |
| `문제정리.md` | 초기 문제·개선 과정 메모 |
| `requirements.txt` | 의존성 |

---

## 부록 B. 학술 키워드 후보

아파트 전세가율, 실거래가, 매매–전세 매칭, 비율 타깃 변환, XGBoost, 시계열 홀드아웃, 래그 피처, 이상치 제거, MAPE, 오차 분포, Feature Importance, Applied Machine Learning
